/**
 * Represents metadata of a video
 */
export interface IVideoMetadata {
  link: string;
  title: string;
  channel: string;
  views: number;
  likes: number;
  uploadDate: string;
  duration: number;
  thumbnail: string;
}
