import React from "react";
import { Box, Typography, Button, ArrowBack } from "@mui/material";

export default () => (
    <Box>
        {/* <Button variant="outlined" startIcon={ArrowBack}>
            Back
        </Button> */}
        <Typography variant="h1" component="h2">
            h1. Heading
        </Typography>
        ;
    </Box>
);
